__version__ = "1.0.0"
APP_NAME = "Universal Controller MIDI"
# Bundle ID stays on the original namespace so existing installs (if any
# beta-tested under the old name) keep their data dir + MIDI port intact.
APP_ID = "design.aidxn.gamepad-midi-bridge"
